type Modelos @model @auth(rules: [{allow: public}]) {
  id: ID!
  Modelurl: String
}

type Preguntas @model @auth(rules: [{allow: public}]) {
  id: ID!
  motivacion: Int
  relajacion: Int
  satisfaccion: Int
  aprendizaje: Int
  estres: Int
  ayuda: Int
  seguridad: Int
  actividadFisica: Int
  amistadFamilia: Int
  gratitud: Int
  pasatiempos: Int
  inspiracion: Int
  reflexion: Int
  comunicacion: Int
  creatividad: Int
}
 